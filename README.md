# Web test

## Run

```bash

# Install dependencies
pnpm --filter <package-name> install

# Run
pnpm --filter <package-name> run dev

# Run Spring module
mvn spring-boot:run -pl <module-name> -am

# Run Spring artifacts
mvn spring-boot:run -pl :<artifact-name> -am

```
